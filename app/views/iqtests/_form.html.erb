<!-- app/views/iqtests/_form.html.erb -->
<%= form_for @iqtest, url: iqtest_path(@iqtest), method: :patch do |f| %>
  <% if @iqtest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@iqtest.errors.count, "error") %> prohibited this iqtest from being saved:</h2>

      <ul>
        <% @iqtest.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit "Update IQ Test" %>
  </div>
<% end %>
