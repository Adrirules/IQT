<!DOCTYPE html>
<html>
  <head>
    <title>IQT</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Si tu utilises preload, assure-toi d'ajouter `as` correctement -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", as: "style" %>
    <%= javascript_pack_tag 'application', 'data-turbo-track': 'reload', as: 'script' %>
  </head>

  <body>
    <% unless @hide_navbar_and_footer %>
      <%= render "shared/navbar" %>
    <% end %>
    <%= render "shared/flashes" %>
    <p class="alert"><%= alert %></p>

    <%= yield %>

    <% unless @hide_navbar_and_footer %>
      <%= render 'shared/footer' %>
    <% end %>
    <%= javascript_pack_tag 'question_handling', 'data-turbo-track': 'reload' %>

  </body>
</html>
